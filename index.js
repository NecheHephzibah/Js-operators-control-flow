// Answer to assignment number 3
 let classGroup = prompt("Enter class group:");
 
 if (classGroup === "Science") {
	console.log("Your subject combination is English, Mathematics, Physics, chemistry, Biology, Technical Drawing");
 } else if (classGroup === "Social Science") {
	console.log("Your subject combination is English, Mathematics, Accounting, Commerce, Marketing, Geography");
 } else if (classGroup === "Art") {
	console.log("Your subject combination is English, Mathematics, Government, Economics, Literature, History");
} else {
	console.log("Invalid class group, you will be taking General Subjects.")
};



// Answer to assignment number 5  

function (num){
	let pwr = 6;
	for (let i = 0; i < num; pwr ** i);

	if (pwr < num) {
		pwr**2;
	}
	console.log("The number " + pwr +"is the power of 2 nearest to " + num);
};

num;